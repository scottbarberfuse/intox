# intox — redirect

This public repo exists only to serve a redirect from the old GitHub Pages URL
`https://scottbarberfuse.github.io/intox/` to the app's current home:

**https://intox.zbrba.com/**

The app now lives on a Cloudflare Worker (source in the private `intox-private`
repo). GitHub Pages here just bounces old links to the new domain.
